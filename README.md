# hairlog

가족 미용 시술 기록 프로토타입.

## 실행

```
npm install
npm run dev
```

`http://localhost:3000` 또는 같은 네트워크에서 `http://<PC-IP>:3000`.

데이터는 `data/data.json`에 저장됩니다. 첫 실행 시 자동 생성.

## 백업

`설정` 탭에서 JSON 내보내기/가져오기.
